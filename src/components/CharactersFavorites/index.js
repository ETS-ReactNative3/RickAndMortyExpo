import React from "react";
import { Text, View } from "react-native";

import { Container } from './styles';

const CharactersFavorites = () => {
  return (
    <Container>
      <Text>Personagens favoritos</Text>
    </Container>
  );
};

export default CharactersFavorites;
